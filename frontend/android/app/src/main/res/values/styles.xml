<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!-- Splash screen — Android 12+ core-splashscreen -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Warna background layar penuh -->
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <!-- Icon logo (transparan background) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <!-- Background di belakang icon (harus sama dengan layar) -->
        <item name="windowSplashScreenIconBackgroundColor">@color/splashBackground</item>
        <!-- Theme setelah splash selesai -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <!-- Fallback window background untuk Android < 12 -->
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

</resources>
