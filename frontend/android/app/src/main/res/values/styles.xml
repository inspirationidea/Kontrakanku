<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!-- Splash screen theme — Android 12+ (core-splashscreen API) -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Background color gelap sesuai tema app -->
        <item name="windowSplashScreenBackground">#0a0712</item>
        <!-- Icon centered di splash (drawable/splash_icon.png) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <!-- Durasi animasi icon (ms) -->
        <item name="windowSplashScreenAnimationDuration">500</item>
        <!-- Theme yang digunakan setelah splash selesai -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <!-- Fallback untuk Android < 12 -->
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

</resources>
